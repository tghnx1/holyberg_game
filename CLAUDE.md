@AGENTS.md

# Claude Code

Use `AGENTS.md` as the shared project-agent instructions and read `docs/codex-handoff.md` before substantial work.

Trust the current repository/Git state over stale documentation, especially when parallel agents are working.
