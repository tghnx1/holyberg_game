import Phaser from 'phaser'; import {DESIGN_WIDTH} from '../constants';
export class RhythmScene extends Phaser.Scene{private score=0;constructor(){super('RhythmScene')}init(data:{score?:number}){this.score=data.score??0}create(){this.cameras.main.setBackgroundColor('#090611');for(let i=0;i<8;i++)this.add.rectangle(260+i*110,470,72,320,Phaser.Display.Color.GetColor(40+i*16,15,80+i*12)).setAngle(i%2?4:-4);this.add.text(DESIGN_WIDTH/2,220,'RHYTHM LEVEL\nCOMING NEXT',{fontFamily:'Archivo Black',fontSize:'62px',color:'#ff5c49',align:'center',stroke:'#421461',strokeThickness:10}).setOrigin(.5);this.add.text(DESIGN_WIDTH/2,350,`FINAL SCORE  ${this.score}\n\nPRESS SPACE OR TAP TO RUN AGAIN`,{fontFamily:'Space Mono',fontSize:'22px',color:'#fff',align:'center'}).setOrigin(.5);const go=()=>this.scene.start('BerlinScene');this.input.keyboard?.once('keydown-SPACE',go);this.input.once('pointerdown',go);this.cameras.main.fadeIn(500);}}
