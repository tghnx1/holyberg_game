# Development track

This short, original click track exists only to exercise the MIDI rhythm engine before the production Ableton export is available. Regenerate `audio.wav` and `chart.mid` with:

```bash
npm run generate:rhythm-fixture
```
